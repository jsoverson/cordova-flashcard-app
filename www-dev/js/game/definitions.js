/*global define, Media, document, window*/

define(
  [
    'game/alphabet/List'
  ],
  function (AlphabetSelection) {
    "use strict";

    return {
      AlphabetSelection : AlphabetSelection
    }

  });